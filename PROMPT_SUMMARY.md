# PROMPT_SUMMARY — 影响项目走向的 8 个关键 Prompt

> 从项目全程（2026-08-21 调研 → 2026-08-24 交付收尾）中选出真正改变走向的 8 条 Prompt，按阶段整理。
> 早期 Session 的原话以当时产出的文档与决策记录为据做**忠实缩写**（标注 ⁂）；开发 Session 起为原话或贴近原话的摘录。
> 完整过程证据见 `AI_WORKFLOW.md`；本文不含内部对话全文与任何账号/密钥信息。

---

## 1. 项目启动：定任务，同时给资源上限（2026-08-21 ⁂）

**我说了什么**：做一个用于展示「我如何与 AI 协作开发」的电商 Demo；先并行做三份调研（电商功能与用例、提效工具与 skills、技术栈与开源参考）——但**大规模调研要限制 agent 数量**。
**为什么这时这样说**：项目的展示重点是协作过程本身，调研要快但不能失控烧量；先给资源边界，避免后面逐次纠偏。
**AI 做了什么**：在限定的并行度内完成三份调研（`docs/research/01–03`），产出功能清单、库存/订单坑位清单、技术栈对比。
**结果/证据**：三份调研文档成为后续所有规划的素材；库存「锁定/扣减/释放」模型即源自调研 01。
**减少了什么**：一次说清「做什么 + 资源上限」，调研阶段零返工，也避免了后续里程碑里再讨论技术选型。

## 2. MVP 收敛：把开放问题一次拍板（2026-08-22 ⁂）

**我说了什么**：把 4–5 周的扩展版规划收敛为 **24–30 小时、10 分钟可完整演示**；当场拍板全部开放决策（本地 Docker + PostgreSQL 16、UI 中文、协作过程用仓库留痕 + 3–5 分钟录屏展示……），并写明「**开发阶段不再询问**」。
**为什么这时这样说**：所有会导致 AI 中途停下来提问的决策，在开发开始前集中清零；范围收敛写成 P0/P1/不做三张清单，防止范围回弹。
**AI 做了什么**：产出收敛版规划（`docs/plan/01`，含决策记录表、超预算规则、M5 启动门槛）。
**结果/证据**：`docs/plan/01` §3 决策表；开发全程 AI 未因产品决策停下过一次。
**减少了什么**：这是「人工介入（产品/技术）0 次」的直接前提——问题在规划期集中回答，而不是开发期零散回答。

## 3. 上下文交接：为全新 Session 造唯一入口（2026-08-22 ⁂）

**我说了什么**：把定稿内容压缩成一份面向**全新 Claude Code Session** 的接手文档 `IMPLEMENTATION_BRIEF.md`（读完即可开工，细节按引用深入），配套 `AI_WORKFLOW.md` 记录模板（随做随记、不得事后补写）和逐功能验收的测试矩阵（`docs/plan/03`）。
**为什么这时这样说**：规划 Session 的上下文无法带进开发 Session；与其依赖对话记忆，不如把上下文固化为仓库内文档，让任何新 Session 冷启动。
**AI 做了什么**：产出 BRIEF v1.1（30 秒摘要 / 已拍板决策 / 数据模型 / 业务规则 / DoD / 工作方式）+ AI_WORKFLOW 模板 + P0 测试矩阵。
**结果/证据**：开发 Session 仅凭这三份文档 + 一条开场 Prompt 即完整接手；`docs/plan/03` 后来逐条对应到 103 条单测与 47 条 E2E。
**减少了什么**：交接零问答；「测试矩阵即规格」让验收标准不随对话漂移。

## 4. Preflight 与自主边界：开工前先验环境、先划红线（2026-08-23 开发 Session 开场）

**我说了什么**：「把 IMPLEMENTATION_BRIEF.md 作为唯一接手入口……不要重新做调研、不要扩大 P0 范围。先完成接手和开发前检查：Git 工作区、Node/包管理器、Docker Postgres、env/依赖/Playwright、六类测试命令、无人值守的认证风险、确认只做 M1–M4。**本地、安全、可逆的问题你直接解决，不需要问我**。只有真实密钥/生产资源、破坏性操作、改变 P0 范围的文档硬冲突、本地无法解决的环境问题才停下来。」
**为什么这时这样说**：无人值守开发最怕两件事——环境问题半夜卡死、AI 拿模糊授权做危险操作；所以把「可以自己修什么 / 必须停什么」写成清单。
**AI 做了什么**：逐项 Preflight（自行拉起 Docker Desktop；发现本机 5432 被占用后改用 5433 并记录决策），随后直接开始 M1。
**结果/证据**：AI_WORKFLOW M1 段「人工决策」表；全程唯一环境调整（端口）由 AI 自行完成并留痕。
**减少了什么**：环境类问题零人工处理；「停下来的条件」清单让后续对真实密钥（M5）、强推等事项的停点全部命中预期。

## 5. 自主开发目标：用「成功条件 + 闭环 + 失败协议」代替过程指挥（2026-08-23，/goal）

**我说了什么**：「完成 M1–M4 全部 P0，以 docs/plan/03 为验收依据。连续自主执行，不等待『继续』。每个里程碑必须完成『实现 → 测试 → 修复 → Diff Review → 更新 AI_WORKFLOW/ADR → 本地 Commit』闭环。普通、局部、可逆的决定用最简单惯用方案并记录。失败时读真实错误、形成根因假设、聚焦修改重测；同一问题 3 个实质不同方案仍失败才记 BLOCKERS.md 并继续其它 P0。成功结束的 8 个条件是……硬停止仅限……」
**为什么这时这样说**：只定义「什么算完成、失败怎么处理、什么时候必须停」，把过程决策全部交给 AI——指挥过程会把自主开发退化成逐步问答。
**AI 做了什么**：按闭环推进 M1–M4；**其间自行决定**用多子代理并行做 API 版本调研与 seed 数据生成、并两次调用只读 Reviewer 审查（这些编排是 Claude 的执行策略，非我的人工分工）；失败均按协议自愈（如 tsx CJS、React 19 表单重置、录屏迭代等），全程未触发 BLOCKERS。
**结果/证据**：AI_WORKFLOW M1–M4 段（含失败与恢复表）；34 个里程碑内提交呈「test 红 → feat 绿」序列。
**减少了什么**：P0 全程零「过程指挥」型对话；返工被约束在 AI 内部循环里，不消耗人工往返。

## 6. 测试与 Review 口径：分层 + 覆盖率下限 + 功能全覆盖（2026-08-23，与开场 Prompt 同时给出）

**我说了什么**：「测试我个人喜欢分层：UT、FT（针对每个 function）、前后端各自与合并的 E2E、Playwright 模拟真人点击 + 开发结束后的本地部署点击测试。UT/FT 覆盖率尽量 ≥80%、最好 ≥85%；E2E 和 Playwright 不好用百分比衡量，但**功能必须全面 cover**——10 个功能 25 个 use case 不能只测 5 个。」
**为什么这时这样说**：覆盖率数字容易造假、用例数量容易偷懒，所以同时给「可度量的下限」和「不可degradation 的枚举式全覆盖」两种口径。
**AI 做了什么**：单测挂覆盖率阈值并主动补测到 Stmts 96.5% / Branch 88.4%；E2E 按 `docs/plan/03` 矩阵逐行落成 47 条；另加录屏冒烟（真人视角走全流程）。
**结果/证据**：`TEST_REPORT.md`（逐文件用例清单）+ `ACCEPTANCE_MATRIX.md`（矩阵逐条附证据）。
**减少了什么**：验收时无需逐条人工检查功能——矩阵映射让「测没测全」变成可 diff 的事实。

## 7. 最终交付：推送前检查 + CI 自愈 + 最小权限边界（2026-08-24）

**我说了什么**：「仓库建好了。先做推送前检查：确认没有 .env、密钥、登录态、本地数据库等隐私文件会被提交；把交付文档里的模型、介入、耗时、Commit 数、CI/PR 状态统一核对。然后设 remote，推 main、dev 和 p0-done tag；CI 失败就读日志做最小修复、本地复验后继续推；全绿后按 docs/FINAL_PR.md 建 dev → main 的 PR，**先不要合并**。**涉及删除历史、修改核心业务逻辑或合并 PR 时再问我**。」
**为什么这时这样说**：对外发布是不可逆边界，检查（隐私）→ 验证（CI）→ 汇报（PR 不合并）分级放行；同时把「必须回来问」的三类操作显式列出。
**AI 做了什么**：完成安全扫描（确认 `.env.local`/登录态从未入库）、统一文档数字、推送后 CI 首跑即绿、创建 PR #1 并保持未合并。
**结果/证据**：GitHub Actions 多轮 success；PR #1 OPEN/MERGEABLE。
**减少了什么**：发布环节一次通过；权限清单让后来的历史重写在正确的停点等待授权，而不是擅自执行。

## 8. 安全收尾：人工抽查 + 定点授权（2026-08-24）

**我说了什么**：「先不要 commit——代码里还有类似 cloud session 这种敏感的东西，先处理再交给我。」确认处理方案后：「历史重写和强推我明确授权；强推前再确认所有 commit message 里没有相关链接，推完等 CI 绿，不要合并 PR。」
**为什么这时这样说**：AI 的自查有盲区（会话链接出现在提交信息模板里而非代码中），人工抽查负责发现问题，但修复权限按「先本地处理、后定点授权」两段发放——历史重写 + 强推是最高风险操作，必须显式授权。
**AI 做了什么**：盘点出 38 条提交尾部的会话链接 → 本地 filter-branch 重写（内容树零差异、留备份分支）→ 停在未推送状态汇报 → 获授权后 `--force-with-lease` 同步并重打 tag，远程逐条复核 0 残留。
**结果/证据**：GitHub 上 dev 全部提交信息无会话链接；CI 重跑全绿；PR 保持未合并。
**减少了什么**：把一次潜在的隐私泄露收敛为一次可回滚、可验证的定点操作；「先处理、不提交、再授权」避免了任何未经确认的破坏性动作。

---

## 一句话规律

这 8 条 Prompt 的共性：**把决策前置（§1–§3）、把边界写成清单（§4/§7）、把目标定义成可验证的完成条件（§5/§6）、把高风险操作留给定点授权（§7/§8）**——AI 负责过程与执行策略（包括它自行选择的多代理编排），人负责定义、边界与抽查。
